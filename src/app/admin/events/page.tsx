/** @format */
import ListEvent from "@/components/event/ListEvent";
import React from "react";

function Events() {
    return (
        <main>
            <ListEvent />
        </main>
    );
}

export default Events;
